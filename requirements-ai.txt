# Optional AI backends. Pick the lines for backends you want.
# These are NOT installed by `pip install -r requirements.txt`.

# OpenAI-compatible (covers OpenAI, OpenRouter, vLLM OpenAI server,
# LM Studio, etc.)
openai>=1.30

# Google Gemini
google-genai>=0.4

# llama.cpp direct GGUF load. The CPU wheel installs out of the box;
# for GPU acceleration use one of the prebuilt CUDA / Metal wheel
# indexes (see https://abetlen.github.io/llama-cpp-python/whl/ ).
llama-cpp-python>=0.2.90

# Ollama uses plain HTTP via `requests`, which is already in the base
# requirements file — no extra package needed here.
